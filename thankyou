<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Scrummer — Thank You</title>
  <link rel="stylesheet" href="css/style.css">

  <style>
    /* Subtle entrance animation */
    .tyWrap{
      animation: fadeUp .45s ease-out both;
    }
    @keyframes fadeUp{
      from { opacity: 0; transform: translateY(10px); }
      to   { opacity: 1; transform: translateY(0); }
    }

    /* Gentle check mark "pop" */
    .tyIcon{
      width: 54px;
      height: 54px;
      margin: 0 auto 10px;
      border-radius: 999px;
      display: grid;
      place-items: center;
      animation: pop .35s ease-out both;
      animation-delay: .12s;
      border: 1px solid rgba(255,255,255,.14);
      background: rgba(255,255,255,.06);
    }
    @keyframes pop{
      0%   { transform: scale(.92); opacity: 0; }
      60%  { transform: scale(1.03); opacity: 1; }
      100% { transform: scale(1); }
    }

    /* Optional: small hover lift on button */
    .tyBtn{
      display: inline-block;
      transition: transform .15s ease, filter .15s ease;
    }
    .tyBtn:hover{
      transform: translateY(-1px);
      filter: brightness(1.05);
    }
  </style>
</head>

<body data-page="feedback">

  <div class="workspace">
    <div class="card tyWrap" style="max-width:600px;margin:auto;text-align:center;">

      <div class="tyIcon" aria-hidden="true">
        <!-- simple check mark -->
        <svg width="22" height="22" viewBox="0 0 24 24" fill="none">
          <path d="M20 7L10 17l-5-5" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
      </div>

      <h2>Thank You 🙌</h2>

      <p style="margin-top:10px;">
        Your feedback has been submitted successfully.
      </p>

      <p style="opacity:.8;">
        Scrummer is evolving based on real practitioner insights like yours.
      </p>

      <div style="margin-top:20px;">
        <a href="index.html" class="btn primary tyBtn">Back to Scrummer</a>
      </div>

    </div>
  </div>

</body>
</html>
